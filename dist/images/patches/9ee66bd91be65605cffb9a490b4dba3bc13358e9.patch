diff --git a/northd/northd.c b/northd/northd.c
index 1ca8dedaf..e6fc96b8b 100644
--- a/northd/northd.c
+++ b/northd/northd.c
@@ -12433 +12433,9 @@ build_route_match(const struct ovn_port *op_inport, uint32_t rtb_id,
-    *priority = (plen * ROUTE_PRIO_OFFSET_MULTIPLIER) + ofs;
+
+    if (is_src_route) {
+        *priority = 1;
+        if ((is_ipv4 && plen == 32) || (plen == 128)) {
+            *priority = 2;
+        }
+    } else {
+        *priority = (plen * ROUTE_PRIO_OFFSET_MULTIPLIER) + ofs;
+    }
